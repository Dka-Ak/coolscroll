user = input("Enter username: \n")
if " " in user:
  user.replace(" ", "-")
elif:
  while user != type(str)
    import random
    usernames = ['fuzzymoose13', "bricksthecat", "ribbitthedog", "rabbit-vs-kale"]
    input(f"This is not a valid username; TypeError- invalid data type. Try another selection. How about {usernames[randint(0, 3)]}")
else:
  while (user != type(str) False) and (" " in user True):
    print("I do not understand. Maybe try again? \n")
